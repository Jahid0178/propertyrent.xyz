import React from "react";
import PropertySearch from "@/components/SearchBar/PropertySearch";

const HeroSection = () => {
  return (
    <section className="hero-section relative">
      <div className="container">
        <PropertySearch />
      </div>
    </section>
  );
};

export default HeroSection;
